var assert = require('should');
var _ = require('lodash');

var fs_content = require('../../app/helpers/fs_content.js');

describe('file system content helper', () => {
  describe('get ui directories', () => {
    it('should return return a list of ui directories in a given pwd');
  });

  describe('get ui data', () => {
    it('should look return the contents of a *ui.json file in a ui directory, with a given pwd');
  });
});

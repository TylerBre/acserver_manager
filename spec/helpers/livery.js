var should = require('should');
var livery = require('../../app/helpers/livery.js');

describe('livery helper', () => {
  describe('find all for car', () => {
    it('should find all installed liveries for a given car');
  });

  describe('find all official liveries for car', () => {
    it('should determine which liveries are official');
  });

  describe('filter ui data', () => {
    it('should find only the important UI data');
  });

  describe('install', () => {
    it('should install the livery to the correct in the content directory');
  });
});